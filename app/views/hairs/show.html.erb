<p id="notice"><%= notice %></p>

<%= render 'profileHair' %>
<p>
  <% @hair.user.images.each do |image| %><br>
    <%= image_tag image.media.url(:medium)%><br>
    <%= button_to "Delete", :action => @destroy%>
  <% end %>
</p>
<%= render 'images/form' %>

<br>
<div id='calendar'></div>

<%= link_to 'Edit', edit_hair_path(@hair) %> |
<%= link_to 'Back', hairs_path %>
