<%= form_for(@unavailable) do |f| %>
  <% if @unavailable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unavailable.errors.count, "error") %> prohibited this unavailable from being saved:</h2>

      <ul>
      <% @unavailable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.text_field :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
