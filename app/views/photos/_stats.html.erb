<% photo.style %>
<% photo.education %>
<% photo.experience %>
<% photo.clients %>
<% photo.equipment %>
<% photo.rate %>
<% photo.zip %>
<% photo.user %>

  <% if photo.user.images.empty? %>
     <%= image_tag "default1.png" %>
  <% else %>
     <%= link_to image_tag(photo.user.images.first.picture, {width: "300px", class: "search-images"}), photo %>
   <% end %>

<% link_to 'Show', photo %>
<% link_to 'Edit', edit_photo_path(photo) %>
<% link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' } %>