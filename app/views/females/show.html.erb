<p id="notice"><%= notice %></p>

<%= render 'profileShow' %>

<p>
  <% @female.user.images.each do |image| %><br>
    <%= image_tag image.media.url(:medium)%><br>
    <%= button_to "Delete", :action => @destroy%>
  <% end %>
</p>

<%= render 'images/form' %>

<%= link_to 'Edit', edit_female_path(@female) %> |
<%= link_to 'Back', females_path %>
